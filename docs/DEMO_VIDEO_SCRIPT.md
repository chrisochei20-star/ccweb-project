# 2-minute demo video — shot list (CCWEB prototype)

Record at **1920×1080**, ~2:00 runtime, clear narration. Emphasize: **signals, not guarantees** for crypto features.

| Time | Scene | Action |
|------|--------|--------|
| 0:00–0:15 | Home | Scroll hero; tap Learn / Find / Build / Earn pillar cards. |
| 0:15–0:35 | Sign up | `/signup` — enter display name, email, password → submit → lands on Dashboard. |
| 0:35–0:50 | Dashboard | Show welcome line with display name; pillar quick links. |
| 0:50–1:10 | Find | `/find` — run token scan; open “Full token detail” if visible. |
| 1:10–1:25 | Early Signals | `/early-signals` — scroll feed; tap a row → Token detail (chart, scores, whale list). |
| 1:25–1:40 | Build | `/dapp-builder` or `/ai-agents` — scroll templates or agents. |
| 1:40–1:55 | Streaming | `/ai-streaming` — show room UI (create/list if time). |
| 1:55–2:00 | Log out | Navbar → Log out; mention sessions reset when API restarts (prototype). |

**Mobile:** repeat key path (login → Find → Early Signals) in narrow viewport or Capacitor build.

**Export:** MP4 (H.264), upload to internal drive or unlisted YouTube for stakeholders — do not commit large binaries to git.
